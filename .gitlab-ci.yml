# GitLab Pages — publishes the static dashboard. No build step (vanilla HTML/CSS/JS).
pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r index.html assets data public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
